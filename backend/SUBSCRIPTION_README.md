# OTPGuard Subscription System 🚀\n\nA comprehensive subscription and billing system for OTPGuard SaaS MFA platform.\n\n## 📋 Overview\n\nThis system implements:\n- **4 Subscription Plans** (Starter, Growth, Business, Enterprise)\n- **Feature Enforcement** via middleware\n- **Usage Tracking** for billing preparation\n- **Rate Limiting** and security controls\n- **Clean Architecture** ready for M-Pesa integration\n\n## 🧩 Subscription Plans\n\n### 1. STARTER (Free)\n- **Price**: 0 KES\n- **Max Users**: 50\n- **OTP Channels**: Email only\n- **Features**: Basic dashboard\n- **SMS**: Disabled\n\n### 2. GROWTH (Most Popular)\n- **Price**: 1,500 KES / $10\n- **Max Users**: 1,000\n- **OTP Channels**: Email + SMS\n- **Features**: Full dashboard, login analytics\n- **SMS Cost**: 2-5 KES per message\n\n### 3. BUSINESS\n- **Price**: 5,000 KES / $35\n- **Max Users**: Unlimited\n- **OTP Channels**: Email + SMS + TOTP\n- **Features**: Admin dashboard, device tracking, geolocation, custom branding\n- **SMS Cost**: 1-3 KES per message\n\n### 4. ENTERPRISE (Custom)\n- **Price**: Custom\n- **Max Users**: Unlimited\n- **Features**: All features + white-label + dedicated support\n- **SMS Cost**: 0.5-2 KES per message\n\n## 🏗️ Architecture\n\n### Models\n```\nPlan\n├── name, display_name, price_kes, price_usd\n├── max_users, otp_channels, features\n└── sms_enabled, sms_cost_min, sms_cost_max\n\nSubscription\n├── user_id, plan_id, status\n├── start_date, end_date, trial_ends\n└── auto_renew\n\nUsageLog\n├── user_id, usage_type, quantity, cost_kes\n└── metadata, timestamp\n\nUsageSummary\n├── user_id, month\n├── email_otp_count, sms_otp_count, totp_count\n└── total_users, total_cost_kes\n```\n\n### Middleware\n- `@require_subscription` - Ensures active subscription\n- `@require_feature(name)` - Checks feature access\n- `@require_channel(type)` - Validates OTP channel\n- `@check_user_limit` - Enforces user limits\n- `@rate_limit_otp()` - Prevents OTP spam\n- `@log_api_usage(type)` - Tracks usage for billing\n\n## 📡 API Endpoints\n\n### Public\n```http\nGET /api/subscription/plans\n# Returns all available plans\n```\n\n### User Management\n```http\nGET  /api/subscription/current\nPOST /api/subscription/subscribe\nPOST /api/subscription/upgrade\nPOST /api/subscription/trial\nPOST /api/subscription/cancel\n```\n\n### Usage & Analytics\n```http\nGET /api/subscription/usage\nGET /api/subscription/usage/history\n```\n\n### Validation\n```http\nGET /api/subscription/check/feature/<feature_name>\nGET /api/subscription/check/channel/<channel_type>\nGET /api/subscription/check/limits\n```\n\n### Admin\n```http\nPOST /api/subscription/admin/plans\nPOST /api/subscription/admin/users/<id>/subscription\n```\n\n## 🔐 Feature Enforcement Examples\n\n### MFA Routes (Updated)\n```python\n# SMS OTP - requires SMS-enabled plan\n@mfa_bp.route('/otp/send', methods=['POST'])\n@api_key_required\n@rate_limit_otp(max_requests=10, window_minutes=5)\n@log_api_usage('sms_otp')\ndef external_send_otp():\n    # Checks plan allows SMS\n    # Tracks usage and cost\n    # Enforces user limits\n```\n\n### TOTP Setup - Business+ only\n```python\n@mfa_bp.route('/totp/setup', methods=['POST'])\n@jwt_required()\ndef totp_setup():\n    if not user.can_use_channel('totp'):\n        return jsonify({\n            'error': 'Your plan does not support TOTP',\n            'code': 'CHANNEL_NOT_AVAILABLE'\n        }), 403\n```\n\n## 💰 Usage Tracking\n\n### Tracked Events\n- `email_otp` - Email OTP sent\n- `sms_otp` - SMS OTP sent (with cost)\n- `totp_verify` - TOTP verification\n- `user_added` - New user added via API\n\n### Cost Calculation\n```python\n# SMS cost based on plan\ncost = SubscriptionService.calculate_sms_cost(user_id)\n\n# Log usage with cost\nSubscriptionService.log_usage(\n    user_id=user_id,\n    usage_type='sms_otp',\n    quantity=1,\n    cost_kes=cost,\n    metadata={'phone': phone}\n)\n```\n\n## 🚀 Setup Instructions\n\n### 1. Install Dependencies\n```bash\n# Already in requirements.txt\npip install -r requirements.txt\n```\n\n### 2. Initialize Subscription System\n```bash\npython init_subscriptions.py\n```\n\n### 3. Test API Endpoints\n```bash\n# Get plans\ncurl http://localhost:5000/api/subscription/plans\n\n# Subscribe to Growth plan\ncurl -X POST http://localhost:5000/api/subscription/subscribe \\\n  -H \"Authorization: Bearer <token>\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"plan_name\": \"growth\"}'\n```\n\n## 🧪 Testing Plan Enforcement\n\n### Test SMS Restriction\n```python\n# User on Starter plan tries SMS OTP\nresponse = client.post('/api/mfa/otp/send', \n    headers={'X-API-Key': 'starter_user_key'},\n    json={'method': 'sms', 'phone': '+254700000000'}\n)\n# Should return 403 with CHANNEL_NOT_AVAILABLE\n```\n\n### Test User Limits\n```python\n# Starter plan user tries to add 51st user\n# Should return 403 with USER_LIMIT_REACHED\n```\n\n### Test TOTP Access\n```python\n# Starter/Growth user tries TOTP setup\nresponse = client.post('/api/mfa/totp/setup',\n    headers={'Authorization': f'Bearer {token}'}\n)\n# Should return 403 for Starter/Growth users\n```\n\n## 📊 Usage Analytics\n\n### Monthly Summary\n```json\n{\n  \"month\": \"2024-04\",\n  \"total_users\": 45,\n  \"email_otp_count\": 1250,\n  \"sms_otp_count\": 340,\n  \"totp_count\": 89,\n  \"total_cost_kes\": 1020.50\n}\n```\n\n### Real-time Usage\n```python\n# Check current usage vs limits\nstats = SubscriptionService.get_usage_stats(user_id)\nuser_count = user.get_user_count()\nplan_limit = user.current_plan.max_users\n\nusage_percentage = (user_count / plan_limit * 100) if plan_limit > 0 else 0\n```\n\n## 🔄 Migration Guide\n\n### Existing Users\n1. Run `init_subscriptions.py` to migrate existing users\n2. Existing users get 14-day Growth trial\n3. Backward compatibility maintained via `user.plan` field\n\n### API Integration\n1. Update frontend to check plan features\n2. Handle subscription-related error codes\n3. Display usage statistics in dashboard\n\n## 💳 M-Pesa Integration Preparation\n\n### Ready for Payment Integration\n```python\n# Webhook placeholder in subscription routes\n@subscription_bp.route('/webhook/mpesa', methods=['POST'])\ndef mpesa_webhook():\n    # Process M-Pesa payment\n    # Update subscription status\n    # Send confirmation\n    pass\n\n# Billing calculation ready\nmonthly_cost = plan.price_kes + (sms_usage * sms_rate)\n```\n\n### Usage-Based Billing\n- SMS costs tracked per message\n- Monthly summaries for billing\n- Cost estimates available via API\n\n## 🛡️ Security Features\n\n### Rate Limiting\n- 5 OTP requests per 5 minutes (internal)\n- 10 OTP requests per 5 minutes (API)\n- Configurable per endpoint\n\n### Plan Validation\n- Middleware enforces all restrictions\n- Graceful error messages\n- Feature flags for easy updates\n\n## 🔧 Configuration\n\n### Environment Variables\n```bash\n# Add to .env\nOTP_RATE_LIMIT_REQUESTS=5\nOTP_RATE_LIMIT_WINDOW=300\nSMS_DEFAULT_COST_KES=3.0\n```\n\n### Plan Customization\n```python\n# Update plans via admin API\nPOST /api/subscription/admin/plans\n{\n  \"name\": \"custom_plan\",\n  \"display_name\": \"Custom Plan\",\n  \"price_kes\": 250000,\n  \"max_users\": 5000,\n  \"otp_channels\": [\"email\", \"sms\"],\n  \"features\": [\"custom_feature\"]\n}\n```\n\n## 📈 Monitoring\n\n### Key Metrics\n- Active subscriptions by plan\n- Monthly recurring revenue (MRR)\n- SMS usage and costs\n- Feature adoption rates\n- User limit utilization\n\n### Alerts\n- Users approaching limits\n- High SMS costs\n- Failed subscription renewals\n- Unusual usage patterns\n\n## 🚨 Error Codes\n\n- `SUBSCRIPTION_REQUIRED` (402) - No active subscription\n- `FEATURE_NOT_AVAILABLE` (403) - Feature not in plan\n- `CHANNEL_NOT_AVAILABLE` (403) - OTP channel not allowed\n- `USER_LIMIT_REACHED` (403) - User limit exceeded\n- `RATE_LIMIT_EXCEEDED` (429) - Too many requests\n\n## 🎯 Next Steps\n\n1. **Frontend Integration**\n   - Update UI to show plan features\n   - Add subscription management page\n   - Display usage statistics\n\n2. **Payment Integration**\n   - Implement M-Pesa STK Push\n   - Add webhook handlers\n   - Create billing dashboard\n\n3. **Advanced Features**\n   - Plan downgrades\n   - Proration calculations\n   - Enterprise custom limits\n   - White-label support\n\n---\n\n**🎉 The subscription system is now ready for production use and M-Pesa integration!**